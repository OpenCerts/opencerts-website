const TranscriptStyles = () => (
  <style>
    {`
    .transcript-content {
      font: 90%/1.6 Helvetica, Arial, sans-serif;
      *font-size: 1em;
    }

    .signature-container{
      width:100%;
      float:right;
      text-align:left;
    }

    .end-of-records{
      text-align: center;
      font-size:0.8em;
    }

    `}
  </style>
);

export default TranscriptStyles;
