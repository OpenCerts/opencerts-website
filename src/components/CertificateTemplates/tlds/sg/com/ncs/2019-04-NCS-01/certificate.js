import { Certificate } from "../root";
import { newBackgroundImg, ncsLogo } from "./resources";

const Template = Certificate({
  backgroundImg: newBackgroundImg,
  logo: ncsLogo
});

export default Template;
// end of file
